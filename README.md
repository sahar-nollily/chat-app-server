# Chat app server
  Chat app server server with Socket.IO for real-time messaging
- Multer for handling file uploads
- Socket events:
  - `new message`
  - `add user`
  - `typing` / `stop typing`
  - `disconnect`
